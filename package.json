{
  "name": "wcag-mcp",
  "version": "2.0.0",
  "description": "WCAG MCP server for agentic systems - powered by official W3C data",
  "type": "module",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.4"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node src/index.js",
    "build:data": "node scripts/fetch-wcag-data.js",
    "update:data": "git submodule update --remote data/wcag && npm run build:data",
    "postinstall": "git submodule update --init --recursive && npm run build:data"
  }
}